import XCTest
@testable import StringConversionTool

final class StringConversionToolTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(StringConversionTool().text, "Hello, World!")
    }
}
